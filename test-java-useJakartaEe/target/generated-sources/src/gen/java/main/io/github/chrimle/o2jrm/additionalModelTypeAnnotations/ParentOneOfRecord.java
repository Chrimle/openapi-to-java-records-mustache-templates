/*
 * Example OpenAPI Spec.
 * An example OpenAPI-spec to generate example Java records.
 *
 * The version of the OpenAPI document: 0.0.1
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 * This class was generated using custom mustache templates from
 * openapi-to-java-records-mustache-templates. For further information,
 * questions, requesting features or reporting issues, please visit:
 * https://github.com/Chrimle/openapi-to-java-records-mustache-templates.
 * Generated with Version: 3.10.0
 *
 */

package io.github.chrimle.o2jrm.additionalModelTypeAnnotations;

import java.util.Objects;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import io.github.chrimle.o2jrm.additionalModelTypeAnnotations.FirstOneOfRecord;
import io.github.chrimle.o2jrm.additionalModelTypeAnnotations.SecondOneOfRecord;
import java.io.IOException;
import java.util.Arrays;



import java.io.IOException;
import java.lang.reflect.Type;
import java.util.logging.Level;
import java.util.logging.Logger;
import java.util.ArrayList;
import java.util.Collections;
import java.util.HashSet;
import java.util.HashMap;
import java.util.List;
import java.util.Map;

import com.google.gson.Gson;
import com.google.gson.GsonBuilder;
import com.google.gson.JsonParseException;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.reflect.TypeToken;
import com.google.gson.JsonPrimitive;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import com.google.gson.JsonDeserializationContext;
import com.google.gson.JsonDeserializer;
import com.google.gson.JsonSerializationContext;
import com.google.gson.JsonSerializer;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.JsonArray;
import com.google.gson.JsonParseException;

import io.github.chrimle.o2jrm.JSON;

@io.github.chrimle.o2jrm.test.annotations.TestAnnotationOne
@io.github.chrimle.o2jrm.test.annotations.TestAnnotationTwo
@io.github.chrimle.o2jrm.test.annotations.TestAnnotationThree
@jakarta.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", date = "2026-07-13T12:57:47.251012200+02:00[Europe/Stockholm]", comments = "Generator version: 7.23.0")
public class ParentOneOfRecord extends AbstractOpenApiSchema {
    private static final Logger log = Logger.getLogger(ParentOneOfRecord.class.getName());

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!ParentOneOfRecord.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'ParentOneOfRecord' and its subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<FirstOneOfRecord> adapterFirstOneOfRecord = gson.getDelegateAdapter(this, TypeToken.get(FirstOneOfRecord.class));
            final TypeAdapter<SecondOneOfRecord> adapterSecondOneOfRecord = gson.getDelegateAdapter(this, TypeToken.get(SecondOneOfRecord.class));

            return (TypeAdapter<T>) new TypeAdapter<ParentOneOfRecord>() {
                @Override
                public void write(JsonWriter out, ParentOneOfRecord value) throws IOException {
                    if (value == null || value.getActualInstance() == null) {
                        elementAdapter.write(out, null);
                        return;
                    }

                    // check if the actual instance is of the type `FirstOneOfRecord`
                    if (value.getActualInstance() instanceof FirstOneOfRecord) {
                        JsonElement element = adapterFirstOneOfRecord.toJsonTree((FirstOneOfRecord)value.getActualInstance());
                        elementAdapter.write(out, element);
                        return;
                    }
                    // check if the actual instance is of the type `SecondOneOfRecord`
                    if (value.getActualInstance() instanceof SecondOneOfRecord) {
                        JsonElement element = adapterSecondOneOfRecord.toJsonTree((SecondOneOfRecord)value.getActualInstance());
                        elementAdapter.write(out, element);
                        return;
                    }
                    throw new IOException("Failed to serialize as the type doesn't match oneOf schemas: FirstOneOfRecord, SecondOneOfRecord");
                }

                @Override
                public ParentOneOfRecord read(JsonReader in) throws IOException {
                    Object deserialized = null;
                    JsonElement jsonElement = elementAdapter.read(in);

                    int match = 0;
                    ArrayList<String> errorMessages = new ArrayList<>();
                    TypeAdapter actualAdapter = elementAdapter;

                    // deserialize FirstOneOfRecord
                    try {
                        // validate the JSON object to see if any exception is thrown
                        FirstOneOfRecord.validateJsonElement(jsonElement);
                        actualAdapter = adapterFirstOneOfRecord;
                        match++;
                        log.log(Level.FINER, "Input data matches schema 'FirstOneOfRecord'");
                    } catch (Exception e) {
                        // deserialization failed, continue
                        errorMessages.add(String.format(java.util.Locale.ROOT, "Deserialization for FirstOneOfRecord failed with `%s`.", e.getMessage()));
                        log.log(Level.FINER, "Input data does not match schema 'FirstOneOfRecord'", e);
                    }
                    // deserialize SecondOneOfRecord
                    try {
                        // validate the JSON object to see if any exception is thrown
                        SecondOneOfRecord.validateJsonElement(jsonElement);
                        actualAdapter = adapterSecondOneOfRecord;
                        match++;
                        log.log(Level.FINER, "Input data matches schema 'SecondOneOfRecord'");
                    } catch (Exception e) {
                        // deserialization failed, continue
                        errorMessages.add(String.format(java.util.Locale.ROOT, "Deserialization for SecondOneOfRecord failed with `%s`.", e.getMessage()));
                        log.log(Level.FINER, "Input data does not match schema 'SecondOneOfRecord'", e);
                    }

                    if (match == 1) {
                        ParentOneOfRecord ret = new ParentOneOfRecord();
                        ret.setActualInstance(actualAdapter.fromJsonTree(jsonElement));
                        return ret;
                    }

                    throw new IOException(String.format(java.util.Locale.ROOT, "Failed deserialization for ParentOneOfRecord: %d classes match result, expected 1. Detailed failure message for oneOf schemas: %s. JSON: %s", match, errorMessages, jsonElement.toString()));
                }
            }.nullSafe();
        }
    }

    // store a list of schema names defined in oneOf
    public static final Map<String, Class<?>> schemas = new HashMap<String, Class<?>>();

    public ParentOneOfRecord() {
        super("oneOf", Boolean.FALSE);
    }

    public ParentOneOfRecord(Object o) {
        super("oneOf", Boolean.FALSE);
        setActualInstance(o);
    }

    static {
        schemas.put("FirstOneOfRecord", FirstOneOfRecord.class);
        schemas.put("SecondOneOfRecord", SecondOneOfRecord.class);
    }

    @Override
    public Map<String, Class<?>> getSchemas() {
        return ParentOneOfRecord.schemas;
    }

    /**
     * Set the instance that matches the oneOf child schema, check
     * the instance parameter is valid against the oneOf child schemas:
     * FirstOneOfRecord, SecondOneOfRecord
     *
     * It could be an instance of the 'oneOf' schemas.
     */
    @Override
    public void setActualInstance(Object instance) {
        if (instance instanceof FirstOneOfRecord) {
            super.setActualInstance(instance);
            return;
        }

        if (instance instanceof SecondOneOfRecord) {
            super.setActualInstance(instance);
            return;
        }

        throw new RuntimeException("Invalid instance type. Must be FirstOneOfRecord, SecondOneOfRecord");
    }

    /**
     * Get the actual instance, which can be the following:
     * FirstOneOfRecord, SecondOneOfRecord
     *
     * @return The actual instance (FirstOneOfRecord, SecondOneOfRecord)
     */
    @SuppressWarnings("unchecked")
    @Override
    public Object getActualInstance() {
        return super.getActualInstance();
    }

    /**
     * Get the actual instance of `FirstOneOfRecord`. If the actual instance is not `FirstOneOfRecord`,
     * the ClassCastException will be thrown.
     *
     * @return The actual instance of `FirstOneOfRecord`
     * @throws ClassCastException if the instance is not `FirstOneOfRecord`
     */
    @SuppressWarnings("unchecked")
    public FirstOneOfRecord getFirstOneOfRecord() throws ClassCastException {
        return (FirstOneOfRecord)super.getActualInstance();
    }

    /**
     * Get the actual instance of `SecondOneOfRecord`. If the actual instance is not `SecondOneOfRecord`,
     * the ClassCastException will be thrown.
     *
     * @return The actual instance of `SecondOneOfRecord`
     * @throws ClassCastException if the instance is not `SecondOneOfRecord`
     */
    @SuppressWarnings("unchecked")
    public SecondOneOfRecord getSecondOneOfRecord() throws ClassCastException {
        return (SecondOneOfRecord)super.getActualInstance();
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to ParentOneOfRecord
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        // validate oneOf schemas one by one
        int validCount = 0;
        ArrayList<String> errorMessages = new ArrayList<>();
        // validate the json string with FirstOneOfRecord
        try {
            FirstOneOfRecord.validateJsonElement(jsonElement);
            validCount++;
        } catch (Exception e) {
            errorMessages.add(String.format(java.util.Locale.ROOT, "Deserialization for FirstOneOfRecord failed with `%s`.", e.getMessage()));
            // continue to the next one
        }
        // validate the json string with SecondOneOfRecord
        try {
            SecondOneOfRecord.validateJsonElement(jsonElement);
            validCount++;
        } catch (Exception e) {
            errorMessages.add(String.format(java.util.Locale.ROOT, "Deserialization for SecondOneOfRecord failed with `%s`.", e.getMessage()));
            // continue to the next one
        }
        if (validCount != 1) {
            throw new IOException(String.format(java.util.Locale.ROOT, "The JSON string is invalid for ParentOneOfRecord with oneOf schemas: FirstOneOfRecord, SecondOneOfRecord. %d class(es) match the result, expected 1. Detailed failure message for oneOf schemas: %s. JSON: %s", validCount, errorMessages, jsonElement.toString()));
        }
    }

    /**
     * Create an instance of ParentOneOfRecord given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of ParentOneOfRecord
     * @throws IOException if the JSON string is invalid with respect to ParentOneOfRecord
     */
    public static ParentOneOfRecord fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, ParentOneOfRecord.class);
    }

    /**
     * Convert an instance of ParentOneOfRecord to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}

